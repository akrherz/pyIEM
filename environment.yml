dependencies:
  # Some of these are not hard requirements, but attempting to avoid pip
  # installing these
  - affine
  - cartopy_offlinedata
  # only a util requirement
  - click
  - coverage
  - cython
  - defusedxml
  - docutils
  - fiona
  - gdal
  - geopandas
  - geos
  # downloading stuff
  - httpx
  # templating
  - jinja2
  - matplotlib-base
  - metpy
  - metar>=1.7.0
  - mock
  - netCDF4
  - numpy
  - pandas
  # webutil
  - paste
  # geoplot usage
  - pillow
  - psycopg
  # parquet format support in geopandas
  - pyarrow
  # Models
  - pydantic
  - pygrib
  - pymemcache
  # Faster shapefile reading
  - pyogrio
  - pyproj
  - pytest
  - pytest-cov
  - pytest-httpx
  - pytest-mpl
  - pytest-runner
  - pytest-xdist
  - rasterstats
  # grid reprojection
  - rasterio
  # interpolation
  - scipy
  - setuptools_scm
  - shapely
  # prevent bad conda solve
  - sphinx>=8
  - sphinxcontrib-napoleon
  - sqlalchemy
  # testing
  - werkzeug
  # iemre
  - xarray
